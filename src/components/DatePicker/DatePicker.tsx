import React from "react";
import {DatePicker as DateTime} from 'antd'
type Props = {};

const DatePicker = (props: Props) => {
  return (
    <div className="date__picker shadow">
      {/* <DateTime open={true} showToday={false} size="small"/> */}
    </div>
  );
};

export default DatePicker;
